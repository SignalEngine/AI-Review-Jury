# benchmark output (your own runs)
ai-review-jury-bench/
jury-tune-run/
# per-user decision ledger — YOUR private notes. This repo is public: never track it.
idea-ledger.md
# per-user self-update state (jury-tune writes these)
panel.conf
.jury-last-tuned
.panel-last-tuned
*.tmp
.DS_Store
panel-bench/out/
